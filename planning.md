# llm ignore this

   **Container & Orchestration:**

- docker (Docker daemon/CLI) — ubiquitous in all skills
- docker-compose — multi-container orchestration

   **Cloud CLIs:**

- aws (AWS CLI) — referenced in deployments
- az (Azure CLI) — referenced in deployments
- gcloud (Google Cloud CLI) — referenced in deployments
- aws-cli — AWS command-line interface

   **Configuration Management:**

- terraform — IaC (infrastructure as code)
- pulumi — IaC with programming languages
- ansible — Configuration management
- puppet — Configuration management
- chef — Configuration management
- salt — Configuration management

   **Package Managers & Build Tools:**

- npm — Node.js package manager
- yarn — JavaScript package manager
- pnpm — Fast package manager
- pip — Python package manager
- poetry — Python dependency management
- pipenv — Python virtual environment
- maven — Java build tool
- gradle — Java/Android build tool
- make — C/C++ build automation

   **YAML/JSON Tools:**

- yq — YAML query/manipulation
- jq — JSON query/manipulation
- yamllint — YAML linting (referenced, has skill: yaml-linting)
- jsonlint — JSON linting

   **Secret Management:**

- vault — HashiCorp Vault (secret management)
- bitwarden-cli — Bitwarden secrets
- 1password-cli — 1Password CLI
- aws-secretsmanager — AWS Secrets Manager

   **Security & Signing:**

- cosign — Container image signing (referenced)
- trivy — Vulnerability scanner
- snyk — Vulnerability scanner
- aqua — Container security
- checkov — Infrastructure security scanning
- sonarqube-scanner — Code quality
- openssl — Cryptography toolkit

   **Observability & Monitoring:**

- prometheus — Metrics (referenced in monitoring)
- grafana — Dashboards/visualization
- elasticsearch — Search and analytics
- logstash — Log processing
- kibana — Log visualization
- splunk — Log analytics
- datadog — Monitoring (proprietary)
- newrelic — Application monitoring
- dynatrace — Application monitoring
- honeycomb — Observability

   **System & Process Management:**

- systemctl — systemd service manager (extensively referenced)
- journalctl — systemd log reader (extensively referenced)
- supervisord — Process supervisor
- pm2 — Node.js process manager
- docker-compose — Docker container management

   **Development Tools:**

- git — Version control (assumed, not a skill)
- curl — HTTP client (ubiquitous)
- wget — HTTP download
- ssh — Secure shell
- scp — Secure copy
- rsync — File sync
- jq — JSON processing (referenced)
- git-lfs — Git large file storage
- gh (GitHub CLI) — GitHub command-line interface
- glab (GitLab CLI) — GitLab command-line interface
- jira (JIRA CLI) — Atlassian JIRA CLI

   **Virtual Environment/Language Management:**

- nvm — Node version manager
- rvm — Ruby version manager
- rbenv — Ruby environment
- pyenv — Python version manager
- asdf — Version manager
- direnv — Directory environment

   **Network & DNS Tools:**

- nslookup/dig — DNS lookup
- telnet/nc — Network connectivity testing
- iptables/firewall — Firewall management
- ufw — Uncomplicated firewall
- netstat/ss — Network statistics
- tcpdump — Network packet capture

   **Compression & Archives:**

- tar — Archive tool
- gzip/bzip2/xz — Compression
- zip/unzip — ZIP archives
- 7z — 7-Zip archives

   **Process Management:**

- ps — Process status
- kill/pkill — Kill processes
- top/htop — Process monitoring
- watch — Command execution watcher
- timeout — Command timeout wrapper

   **Text Processing:**

- sed — Stream editor
- awk/gawk — Text processing
- grep — Text search
- perl — Perl scripting
- cut — Column extraction
- tr — Character translation
- sort/uniq — Sorting and uniqueness

   **Databases:**

- psql — PostgreSQL CLI
- mysql — MySQL CLI
- mongosh — MongoDB CLI
- redis-cli — Redis CLI

**Configuration Management:**

- kubectl — Kubernetes CLI (extensively referenced)
- helm — Kubernetes package manager (extensively referenced)
- kustomize — Kubernetes configuration management
- skaffold — Kubernetes development workflow
- tilt — Local Kubernetes development
- flux — GitOps for Kubernetes
- argocd — GitOps CD for Kubernetes
- containerd/podman — Docker alternatives
- cri-o — Container runtime

**Configuration Management:**

- terraform — IaC (infrastructure as code)
- pulumi — IaC with programming languages
- ansible — Configuration management
- puppet — Configuration management
- chef — Configuration management
- salt — Configuration management

   **Package Managers & Build Tools:**

- npm — Node.js package manager
- yarn — JavaScript package manager
- pnpm — Fast package manager
- pip — Python package manager
- poetry — Python dependency management
- pipenv — Python virtual environment
- maven — Java build tool
- gradle — Java/Android build tool
- make — C/C++ build automation

   **Secret Management:**

- vault — HashiCorp Vault (secret management)
- bitwarden-cli — Bitwarden secrets
- 1password-cli — 1Password CLI
- aws-secretsmanager — AWS Secrets Manager

   **Security & Signing:**

- cosign — Container image signing (referenced)
- trivy — Vulnerability scanner
- snyk — Vulnerability scanner
- aqua — Container security
- checkov — Infrastructure security scanning
